#!/bin/bash

ENVFILE='.pico_env'
BUILD_DIR='build.arm'


cat << EOF > $ENVFILE

. ~/.bashrc

export PICO_SDK_PATH=~/pico-sdk

mkdir -p $BUILD_DIR
cd $BUILD_DIR
cmake ..

EOF

/bin/bash --init-file $ENVFILE -s
rm -f $ENVFILE

